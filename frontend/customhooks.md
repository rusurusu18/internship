Custom Hooks
//user data eg: name, role and profile data 
navbar , profile , dashboard

//useEffect //axios get

-> It is a reusable function that contains react hooks (useState, useEffect) avoid duplicating logic  code in different components.

//rules of custom hooks
1. the Function name must start with use
2. It cam use other react hooks.
3. It can call them only inside the react components other custom hooks
4. Don't use or call them inside any loops or conditions 


//Assignment what is terniary operator and HoC(High order Components) with example 

//Local Storage
Local storage stores data inside the browser with example and custom hook example by own debounce etc 